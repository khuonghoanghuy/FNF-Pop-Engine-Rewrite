package states.playstate;

// A Class for Charting State
class PlayTest extends PlayCore
{
    override function create() {
        super.create();
    }

    override function update(elapsed:Float) {
        super.update(elapsed);
    }
}